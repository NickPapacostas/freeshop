<div id="calendar"></div>
<%= form_for @appointment do |f| %>
	<div>Create Appointment</div>
	<div class="divider"></div>
	<%= f.label "Date and Time (choose on calendar)" %>
	<%= f.datetime_local_field :datetime %>
	<%= f.submit "Create appointment" %>
<% end %>

