<div class="container">
	<div class="columns">
		<div class="column col-5">
			Today's Appointments
			<div id="todays-appointments"></div>
		</div>
		<div class="divider-vert"></div>


		<div class="column col-5 text-center">
			<%= link_to("Checkout", new_checkout_path, class: 'btn btn-primary') %>
			<%= link_to("Schedule an appointment", new_appointment_path, class: 'btn btn-primary') %>
			<%= link_to("Create a membership", new_membership_path, class: 'btn btn-primary') %>

			<div class="divider"></div>

			<div id="timeslot-view" class="d-invisible">
				<div id="current-timeslot"></div>
				<div>
				  <table id="timeslot-appointments-table" class="table table-hover">
				    <thead>
				      <tr>
				        <th>Name</th>
				        <th># People</th>
				      </tr>
				    </thead>
				    <tbody>
				    </tbody>
				  </table>
				</div>

		</div>
		</div>

	</div>
</div>